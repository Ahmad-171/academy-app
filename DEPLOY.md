# دليل تجهيز الموقع للنشر — NZ Academy

هذا الدليل يشرح الخطوات المتبقية لتحويل الموقع من نسخة تجريبية إلى نسخة آمنة جاهزة للنشر.
كل خطوات الكود جاهزة؛ يتبقّى تنفيذ خطوات Supabase أدناه **بالترتيب**.

> ⚠️ مهم: بعد البدء بهذه الخطوات، لن يعمل تسجيل الدخول القديم (111/111 إلخ) إلا بعد
> اكتمال الترحيل وإنشاء حسابات المصادقة. نفّذ الخطوات كاملة قبل إعادة النشر النهائي.

---

## 1) تعطيل تأكيد البريد في Supabase
لأن الحسابات تستخدم بريدًا داخليًا اصطناعيًا (`{رقم_الهوية}@academy.local`):

`Supabase Dashboard → Authentication → Providers → Email → عطّل "Confirm email" → Save`

## 2) تشغيل سكربتات SQL بالترتيب
افتح `Supabase → SQL Editor` ونفّذ الملفات بهذا الترتيب (إن لم تكن نفّذت السابقة):

1. `supabase/05_extended_fields.sql`
2. `supabase/06_admin_features.sql`
3. `supabase/07_media.sql`
4. `supabase/08_news_home.sql`
5. `supabase/09_auth_setup.sql`  ← يضيف عمود الربط والدوال المساعدة

> لا تُشغّل `10` و`11` بعد — بعد سكربت الترحيل.

## 3) إنشاء bucket للوسائط (إن لم يكن موجودًا)
`Supabase → Storage → New bucket → الاسم: media → Public: مفعّل`

## 4) ترحيل الحسابات الحالية إلى نظام المصادقة
من جهازك، داخل مجلد المشروع:

```bash
npm install            # مرة واحدة إن لزم
node scripts/migrate-to-auth.mjs
```

سيُنشئ حساب دخول لكل مستخدم موجود بكلمة سره الحالية ويربطه بملفه.
تأكد أن الناتج يظهر ✅ لكل الحسابات.

## 5) تفعيل الحماية (RLS)
بعد نجاح الترحيل، شغّل في SQL Editor:

1. `supabase/10_rls_policies.sql`  ← يفعّل الحماية على كل الجداول

جرّب تسجيل الدخول بحساب حقيقي (نفس رقم الهوية وكلمة السر القديمة) وتأكد أن كل شيء يعمل
لكل دور (مدير، مدرب، لاعب، ولي أمر).

## 6) حذف عمود كلمة السر القديم
بعد التأكد أن تسجيل الدخول يعمل:

1. `supabase/11_finalize_drop_password.sql`  ← يحذف عمود `password` نهائيًا

## 7) إعادة النشر
`Vercel → Redeploy`

---

## بعد النشر (اختياري لكن مُوصى به)
- اربط **نطاقًا مخصصًا** بدل رابط `.vercel.app` من إعدادات Vercel → Domains.
- غيّر كلمات سر حسابات الإدارة إلى كلمات قوية.
- احذف أي حسابات تجريبية لم تعد مطلوبة من لوحة الإدارة.

## ملاحظات أمان
- كلمات السر أصبحت محفوظة في Supabase Auth فقط (لا في جدول قاعدة البيانات).
- لم تعد أي بيانات تُجلب قبل تسجيل الدخول.
- الوصول للبيانات المالية والملاحظات والتقييمات مقيّد حسب الدور والصلاحيات.
- المستخدمون المسجّلون يمكنهم رؤية الأسماء والأدوار الأساسية لبعضهم (لازم لعمل القوائم)؛
  البيانات الحساسة (المالية، الملاحظات، التقييمات، سجل الحضور) مقيّدة بحسب الصلاحية.
