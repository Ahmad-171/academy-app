# دليل تجهيز الموقع للنشر — NZ Academy

كل خطوات الكود جاهزة. المتبقّي تنفيذه **أنت** في Supabase، وكله عبر SQL.

> ⚠️ بعد البدء، لن يعمل تسجيل الدخول القديم إلا بعد اكتمال كل الخطوات.

---

## أين أشغّل SQL؟
افتح مشروعك في **Supabase → القائمة الجانبية → SQL Editor → New query**،
الصق محتوى الملف، ثم اضغط **Run** (أو Ctrl/Cmd + Enter). كرّر لكل ملف بالترتيب.

## أين تجد ملفات SQL؟
كلها داخل مجلد `supabase/` في مستودع الكود على GitHub. افتح الملف، انسخ محتواه كاملًا، والصقه في SQL Editor.

---

## الترتيب (شغّل كل ملف مرة واحدة، بالترتيب)

| # | الملف | ماذا يفعل |
|---|---|---|
| 1 | `supabase/05_extended_fields.sql` | حقول إضافية (إن لم تكن نفّذتها سابقًا) |
| 2 | `supabase/06_admin_features.sql` | جداول الحضور/التقييم/الملاحظات/الأكواد |
| 3 | `supabase/07_media.sql` | عمود الوسائط + سياسات التخزين |
| 4 | `supabase/08_news_home.sql` | عمود «إظهار في الرئيسية» |
| 5 | `supabase/09_auth_setup.sql` | ربط الحسابات بالمصادقة + دوال الصلاحيات |
| 6 | `supabase/09b_migrate_users.sql` | **ينشئ حسابات الدخول** لكل المستخدمين الحاليين (بديل SQL — لا تحتاج node) |
| 7 | `supabase/10_rls_policies.sql` | يفعّل الحماية (RLS) على كل الجداول |
| 8 | `supabase/11_finalize_drop_password.sql` | يحذف عمود كلمة السر القديم نهائيًا |

> الملفات 1–4 غالبًا نفّذتها مسبقًا (الميزات تعمل عندك). كلها آمنة لإعادة التشغيل.

## خطوة واحدة خارج SQL: bucket الوسائط
إن لم يكن موجودًا: `Supabase → Storage → New bucket → الاسم: media → Public: مفعّل`.

## بعد الملف 6 (تحقّق سريع)
شغّل هذا الاستعلام — يجب أن تكون النتيجة **0**:
```sql
select count(*) from public.users where auth_uid is null;
```
لو ظهر رقم أكبر من صفر، فيه حسابات بلا كلمة سر — راجعها قبل المتابعة.

## بعد الملف 7 (اختبر الدخول)
جرّب تسجيل الدخول بحساب حقيقي (نفس رقم الهوية وكلمة السر القديمة) لكل دور
(مدير، مدرب، لاعب، ولي أمر) وتأكد أن كل شيء يعمل. **لا تشغّل الملف 8 إلا بعد نجاح الاختبار.**

## أخيرًا
`Vercel → Redeploy`.

---

## بعد النشر (مُوصى به)
- اربط **نطاقًا مخصصًا** بدل رابط `.vercel.app` من Vercel → Domains.
- غيّر كلمات سر حسابات الإدارة إلى كلمات قوية.

## ملاحظات أمان
- كلمات السر أصبحت في نظام المصادقة فقط (مشفّرة)، ويُحذف العمود القديم.
- لا تُجلب أي بيانات قبل تسجيل الدخول.
- المالية والملاحظات والتقييمات وسجل الحضور مقيّدة حسب الدور والصلاحية.
- المستخدمون المسجّلون يرون الأسماء والأدوار الأساسية لبعضهم (لازم لعمل القوائم)،
  أما البيانات الحساسة فمقيّدة.
