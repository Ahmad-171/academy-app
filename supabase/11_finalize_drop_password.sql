-- ══════════════════════════════════════════════════════════════
-- الخطوة الأخيرة: حذف عمود كلمة السر نهائيًا من جدول users.
-- بعد الترحيل صارت كلمات السر محفوظة في Supabase Auth فقط.
-- شغّل هذا فقط بعد التأكد من نجاح تسجيل الدخول بالحسابات المُرحّلة.
-- ══════════════════════════════════════════════════════════════

alter table users drop column if exists password;
